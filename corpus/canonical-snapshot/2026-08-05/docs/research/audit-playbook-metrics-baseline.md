# Audit: playbook-compiler phase-0 metrics baseline -- what history already exists

2026-07-30. Read-only inventory for specs/40-features/playbook-compiler.intake.md phase-0
telemetry plan. Question: does the historical record already answer the owner's
correlation goals (tokens/delegation, defects in delegated work, rework/rejection
rates), or does phase-0 need new instrumentation?

## 1. Data source table

| Store | Records | Coverage window | Rows | Queried today via |
|---|---|---|---|---|
| `.harness/state/cost-metrics.json` (schemaVersion 1) | Append-only ring buffer, kind in {delegation, gate, chat-turn, workflow}. delegation: model, agentType, target, estTokens, toolUses, durationS, costUsd, title, outcome (kept/partial/rejected/reworked, nullable). gate: gate name, status, durationS, checks, slowest[]. chat-turn: engine/model/effort, inTokens/outTokens/cacheReadTokens, costUsd, replyChars. workflow: workflowId, workers, graph, economy, costBasis. | delegation 2026-07-13..2026-07-30 (n=251); gate 2026-07-28..2026-07-30 (n=150, short window, recent recording start, and store is a hard 500-record ring, MAX_RECORDS); chat-turn 2026-07-15..2026-07-23 (n=75, already 7 days stale, actively evicted); workflow 2026-07-14..2026-07-28 (n=24). | 500 total (ring cap) | `harness.py delegation` writes (record_delegation); `harness.py metrics` reads via `cost_metrics.summarize()` -> delegations.byModel/byAgentType/byOutcome/byModelCTS (cost-to-useful-outcome = sum(estTokens where outcome present)/(kept+reworked)); `scripts/harness_lib/track_record.py summarize_track()` groups delegation rows by model+agentType into kept/partial/rejected/reworked counts + usefulRate (useful=kept+reworked), gated on MIN_N=8 judged delegations before labeling light/standard/heavy burden (else insufficient-data). |
| `.harness/state/search-telemetry.jsonl` | One line per Grep/Glob call intercepted by the search-guardrail hook: ts, session, tool, pattern, scoped(bool), action(blocked/advised/allowed), refresh(bool, optional). | 2026-07-17..2026-07-30 (n=3100 parseable; 2 malformed lines mid-file). | 3102 lines, 3100 parse | No CLI reader/summarizer found -- raw event log, not aggregated by any script today. |
| `.harness/runs/archive/gate-perf.<ts>.jsonl(.manifest.json)` | One line per gate run: at, rows[] of scenario/attempt(parallel/serial)/snapshotS/subprocessS/restoreS/rc/faithful/headSha/affected/cacheHit/cacheMiss/wouldSkip/falseSkip/wouldSaveS. Per-scenario timing/affected-set fidelity, NOT delegation-linked. | 3 archived files: 2026-07-21, 2026-07-25, 2026-07-29 (only whatever survived rotation). | 217+118+101 = 436 lines across 3 files. | `scripts/harness_lib/gate_perf.py` (perf regression checks); no session/agent join key, cannot attribute gate cost to a specific delegated task. |
| `.harness/state/worklog.json` | schemaVersion + entries[] -- free-text kind/title/body/refs/tags ledger (SPEC-112 append-only intent/decision log), not a metrics store. | Spans repo history (not sampled beyond schema here). | not counted | `harness.py log` (write), `harness.py records search/recent/summarize` (read). Text-minable but no structured outcome/token fields. |
| `.harness/state/quality-state.json` | Single mutable snapshot (not a series): lastValidation{gate, status, validatedAt, counts{pass/fail/skip}, stagedFingerprint, policyVersion, requiredProfile, executedProfile}, qualityGates flags. | Current-state only, overwritten each gate run -- no history retained (last write 2026-07-29 here). | 1 | `harness.py verify-status`/gate flow reads/writes directly; not a time series. |
| `scripts/harness_lib/track_record.py` | Derived view over cost-metrics.json delegation rows -- not its own store. | Same as cost-metrics delegation window. | n/a (derived) | Consumed by `harness.py metrics` output via `cost_metrics._track_record()`. |
| `scripts/harness_lib/context_diet.py` | NOT a data store -- a static vendor-flag mapping module. Its only "measurement" is a doc-comment: a one-time 2026-07-15 haiku probe (full diet 40,264 -> 16,520 tok/turn, -59%), hardcoded in the comment, never re-measured or logged per-session. | Single anecdotal datapoint, 2026-07-15. | 0 structured rows | Not queryable -- it is a comment, not telemetry. |

Other candidates checked and ruled out: `baseline_catalog.*` exists only as compiled `.pyc` (source file absent, dead/stale, not a live store). No persisted store found for `review --plan` WARN rows (`overseer_review.py` builds rows in-memory and prints them; no write_json/append_event call found in that module) or for oracle-mutate survivors (no jsonl/write_json sink found under that name). `reckon`/`mutate` appear across several `harness_lib/*.py` files but as verdict consumers (gate_checks_policy.py, agent_parity.py, etc.), not as places that persist a WARN/survival series.

## 2. Owner-metric mapping

(a) tokens per delegation -- ANSWERED by `cost-metrics.json` delegation.estTokens (a chars/4 estimate per the module's own doc-comment; `observedTokens` exists as a field on chat-turn rows and capsule_cost() output but is not populated on delegation rows) aggregated via `byModel`/`byModelCTS`. Caveat: the store is a 500-row ring; at 251 delegation rows already consuming half the ring, effective delegation-only history shrinks as gate/chat-turn/workflow rows compete for the same 500 slots.

(b) defects in delegated work -- PARTIALLY ANSWERED. `outcome` (kept/partial/rejected/reworked) on delegation rows is the closest existing defect signal; `_cts()`/track_record already compute usefulRate = (kept+reworked)/judged. GAP: review --plan WARN rows and oracle-mutate survival counts -- the two other candidates the owner's framing named -- are NOT persisted anywhere found (print-only, no jsonl/state sink); reckon verdicts are consumed by gate-policy code but no dedicated verdict-history store was located. "Defects" today = the coarse 4-way outcome label only.

(c) delegation outcome rates over time -- PARTIALLY sufficient for a BEFORE baseline: 251 delegation rows spanning 2026-07-13..2026-07-30 (17 days), outcome distribution kept=215, rejected=24, reworked=8, partial=3, 1 null -- real signal, and track_record's own MIN_N=8 threshold already treats this as enough to escape "insufficient-data" for the dominant model+agentType groups. GAP: the 500-row hard ring cap means "over time" trending will silently truncate -- gate rows alone are 150 of the 500 slots as of this snapshot covering only 2 days, and chat-turn's window already stops 2026-07-23 (7 days stale), evidence that eviction of older delegation history is happening NOW.

(d) injected prompt bytes per role per session -- GAP, confirmed single-datapoint. No store found (cost-metrics.json has no byte/prompt-injection field; search-telemetry.jsonl tracks search calls, not injection payloads) beyond the one static 2026-07-15 comment in context_diet.py and the 2026-07-29 audit snapshot referenced by the intake doc (`audit-playbook-injection-surfaces.md`, not independently re-verified in this pass). This is the one metric with zero time-series history -- a snapshot audit, not a meter.

## 3. Verdict

Historical record is sufficient as a phase-0 BEFORE-baseline for (a) tokens/delegation and (c) outcome-rate trends, with a caveat: the 500-row ring on cost-metrics.json is actively evicting older rows (chat-turn already 7 days stale) and will keep shrinking the usable window as gate-record volume grows -- any BEFORE snapshot for the compiler comparison should be exported/frozen from the ring NOW, not re-derived later from a post-compiler state of the same file.

It is NOT sufficient for (b) defects beyond the coarse kept/partial/rejected/reworked label -- review-WARN and mutate-survival signals exist as code but not as persisted series, so any defect-rate claim finer than the 4-way outcome bucket needs a new sink added where overseer_review.py and the mutate-oracle already compute their verdicts in memory (cheap -- the computation exists, only persistence is missing).

It is NOT sufficient for (d) injected bytes/session -- genuinely absent as a time series; the intake's own phase-0 scope item (injected bytes/tokens per role per session, recorded before compiler code) is correctly scoped, since today it is one manual audit snapshot, not an ongoing measurement.

Minimal dedicated meter for AFTER-vs-baseline (matches the intake's own ask): (1) append to whatever hook injects role prompts today, logging ts/role/session/bytesInjected/sourceCount to a new jsonl (mirrors the search-telemetry.jsonl pattern already in this repo) -- closes gap (d); (2) one append_event/write_json call each where overseer_review.py assembles WARN rows and where the mutate-oracle finalizes survivors -- closes gap (b); (3) no new work needed for (a)/(c) -- export the current cost-metrics.json delegation slice as the frozen BEFORE snapshot given the ring is live-evicting.
