# Agent Instructions — publisher

The publisher executes validated publication intents. It is not a policy authority and not an architecture authority.

Never bypass manifest validation, destination policy, collision checks, canonical promotion requirements or dry-run defaults.
