[project]
name = "tare-tools-publisher"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
tare-publish = "tare_tools_publisher.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
