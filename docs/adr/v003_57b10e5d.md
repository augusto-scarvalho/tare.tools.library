# SÍNTESE ARQUITETURAL CANÔNICA (MESA REDONDA TRIPARTITE — RODADA 2)

**Para:** Assentos da Mesa Redonda (Google Gemini, Anthropic Claude, OpenAI GPT) e Operador Humano  
**De:** Antigravity Mediator (Independent Synthesis & FSM Governor)  
**Assunto:** Resolução Integral dos Bloqueadores da Rodada 2 e Emissão do **ADR-045 (v003 — Ratificado)**

---

### Resumo Executivo das Resoluções Incorporadas na v003:

1. **Hierarquia Estrita de Autoridade de Estado (`§1.2` & `§2.1`):**  
   Declarada a separação tripartite de autoridade: **Git** é a fonte da verdade do código; **`tare-backlog-graph`** é a fonte exclusiva da verdade do DAG de tarefas (mantendo **zero dependências externas e zero chamadas a binários Git**); e **`tare-kernel`** é o único orquestrador, reconciliador e verificador de evidências Git e atestações Notary.
2. **Protocolo de Recuperação por Alcançabilidade (*Reachability Probe*) (`§3.2`):**  
   Substituição da verificação literal de `Remote HEAD` por uma consulta de alcançabilidade (`git merge-base --is-ancestor <sha> origin/<branch>` e varredura de trailers `Tare-Train-Id`). O predicado unificado elimina falsos negativos causados por avanços concorrentes da branch alvo (`ISS-OPENAI-R2-02` e `ISS-ANTHROPIC-R2-01`).
3. **Proibição de Lock Stealing Descoordenado (`§3.3`):**  
   É terminantemente vedado o roubo de lease enquanto houver um journal pendente em `COMMITTED` ou `PUSHED`. O novo concorrente é obrigado a atuar como *cooperative recovery worker*, conduzindo a saga anterior até `RECONCILED` ou aborto comprovado antes de assumir o lock.
4. **Reconciliação Atômica por `train_id` e Tabela de Idempotência (`§3.5`):**  
   Eliminação do parâmetro ambíguo `force_reconciliation`. O `graph_ops.py` implementa `reconciliation_mode: RECONCILE_RECORDED_INTENT`, persistindo um `landing_record` atômico indexado por `train_id`. Em caso de mutações concorrentes incompatíveis nas tarefas do trem, o backlog rejeita a operação com `409 RECONCILIATION_CONFLICT` determinístico para intervenção humana (`ISS-OPENAI-R2-01`).
5. **Padronização Criptográfica do Release Envelope (RFC 8785 JCS) (`§4.1`):**  
   Definição da serialização canônica JSON Canonicalization Scheme (RFC 8785) com amarração explícita de `key_id`, garantindo reprodutibilidade determinística byte-a-byte na assinatura e verificação Ed25519.
6. **Expansão para 6 Falsificadores Executáveis Obrigatórios & Chaos Injector (`§6.2` & `§7`):**  
   Inclusão de `TEST-FALSIFIER-CRASH-PRE-WAL-UPDATE`, `TEST-FALSIFIER-CONCURRENT-BACKLOG-MUTATION` e `TEST-FALSIFIER-BACKLOG-NO-GIT-SANDBOX`, além de um motor de injeção de falhas (*chaos injector*) em offsets de WAL na Fase 1.

---

```markdown
# ADR-045: North Star do Ecossistema tare.tools & tare-kernel (Agent OS 2.0) — Arquitetura Modular em 4 Repositórios

- **Status:** Ratificado e Aprovado em Síntese Canônica Final (Consenso Pleno Tripartite)
- **Referência:** `CASE-2026-08-17-ECOSYSTEM-AND-KERNEL-NORTH-STAR-V3`
- **Data:** 2026-08-18
- **Autores:** Antigravity Mediator (Consenso Tripartite: Google Gemini 3.7 Flash High, Anthropic Claude Fable 5 High, OpenAI GPT-5.6 Sol High; sob governança do Operador Humano)
- **Escopo:** Ecossistema Global `tare.tools` & `tare-kernel` (Fronteiras de Repositório, Hierarquia de Autoridade, FSM de Landing com WAL, Protocolo de Recuperação por Alcançabilidade, Custódia Notary RFC 8785 e Bateria de Falsificadores)

---

## 1. Contexto, Motivação, Hierarquia de Autoridade & Via Negativa

O ecossistema `tare.tools` evoluiu ao longo de mais de 80 sessões de engenharia, comprovando a viabilidade de swarms multi-agente assíncronos. Contudo, o protótipo v1 atingiu os limites do **monólito acidental**:
1. **Contenção de Estado:** O motor de DAG de tarefas, harness de ferramentas, indexação de código e esteira de releases competiam pelo mesmo repositório e pelo mesmo arquivo compartilhado (`work-graph.json`).
2. **Amnésia Causal:** Falta de amarração criptográfica determinística entre intenção (Specs), execução (Código/Patches) e validação (Testes).
3. **Fragilidade de Concorrência e Crash Recovery:** Ausência de Write-Ahead Logging (WAL), leases temporizados e semântica de saga idempotente para o pouso de alterações concorrentes.

Este ADR estabelece a **North Star definitiva de Produto e Arquitetura do Ecossistema tare.tools e do `tare-kernel` (Agent OS 2.0)**, consolidando uma arquitetura modular, formalmente verificável, resiliente a falhas e orientada à máxima velocidade de iteração.

---

### 1.1 Hierarquia de Autoridade de Estado

Para erradicar ambiguidades de domínio e violações de fronteira, a custódia da verdade é particionada de forma estrita e unidirecional:

| Domínio de Estado | Autoridade Exclusiva | Responsabilidade e Garantias |
| :--- | :--- | :--- |
| **Código & Histórico** | **Git Remoto** | Fonte única da verdade para árvores de código (`tree_hash`), commits e histórico linear/ramificado. |
| **Tarefas & Topologia** | **`tare-backlog-graph`** | Fonte única da verdade para ciclo de vida de tarefas (`TASK-*`), topologia do DAG, precedência e `dag_rev`. **Zero dependência de Git ou rede.** |
| **Orquestração & Efeito** | **`tare-kernel`** | Único verificador de evidências Git, validador de assinaturas Notary e reconciliador da FSM. Orquestra a convergência entre Código e DAG. |

---

### 1.2 Não-Objetivos Explícitos (Via Negativa & Invariantes de Design)
1. **Proibido Monólito Acidental:** Os 4 repositórios possuem fronteiras de domínio rígidas e isoladas; nenhum componente viola a autoridade de estado de outro.
2. **Proibida Escrita Não-Coordenada no Backlog:** O `tare-kernel` nunca edita diretamente o `work-graph.json`. Todas as mutações passam pela interface transacional e tipada de `graph_ops.py`.
3. **Proibido Acoplamento Circular:** O fluxo de dependência entre repositórios é um Grafo Dirigido Acíclico (DAG) estrito verificado em CI via `boundary-lint`.
4. **Proibida Re-inferência em Portões de Paridade:** Replays de validação e cutover usam exclusivamente artefatos gravados e hashes de árvores determinísticas, sem re-inferência de LLMs.
5. **Proibido Landing sem Serialização e Atribuição Causal:** É proibido push concorrente sem comparação atômica de base/árvore e lease lock serializado por branch.
6. **Proibido Lock Stealing Cego:** É vedado roubar leases de branch sem antes auditar o WAL pendente e conduzir a saga anterior a um estado terminal verificado.

---

## 2. Arquitetura Modular em 4 Repositórios & Grafo de Dependências

```
┌───────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                       ECOSSISTEMA TARE.TOOLS                                      │
└───────────────────────────────────────────────────────────────────────────────────────────────────┘
                                                  │
         ┌────────────────────────┬───────────────┴───────────────┬────────────────────────┐
         │                        │                               │                        │
         ▼                        ▼                               ▼                        ▼
┌──────────────────┐    ┌──────────────────┐            ┌──────────────────┐    ┌──────────────────┐
│   tare-kernel    │    │  tare-specgraph  │            │tare-backlog-graph│    │tare.tools.research│
│  (Agent OS Core) │    │(Project Intel/SDD│            │  (Task DAG Engine│    │(Papers, ADRs &   │
│                  │    │  & Traceability) │            │  & Topology)     │    │ Research Memory) │
│ • 5 Planos       │    │ • CPG AST Parser │            │ • DAG Acíclico   │    │ • Artigos IEEE   │
│ • Relay Mesh     │    │ • Matriz Causal  │            │ • Cálculo de     │    │ • ADRs Formais   │
│ • Sandboxes      │    │ • Drift Detection│            │   Fronteira      │    │ • GitHub Pages   │
│ • Orquestração   │    │ • Context Engine │            │ • 79 Testes Unit.│    │ • Benchmarks     │
└──────────────────┘    └──────────────────┘            └──────────────────┘    └──────────────────┘
```

### 2.1 Matriz Estrita de Propriedade de Estado

| Repositório | Estado Exclusivo | Dependências Permitidas | Interface Exposta |
| :--- | :--- | :--- | :--- |
| **`tare-backlog-graph`** | `work-graph.json` (topologia do DAG, estado de tarefas, `landing_records`). | **Zero dependências externas** (núcleo autônomo, sem git, sem network). | CLI / IPC JSON (`graph_ops.py` v1). |
| **`tare-specgraph`** | Matriz causal de rastreabilidade (Specs $\leftrightarrow$ Code $\leftrightarrow$ Tests), CPG AST cache. | `git` (somente leitura de árvores). | CLI / Query Engine. |
| **`tare-kernel`** | FSM de execução de trens, WAL local de landing, ciclo de vida de sandboxes. | `tare-backlog-graph`, `tare-specgraph`, `tare.tools.research`. | CLI unificada `tare`, Daemon, Web Cockpit (:8765). |
| **`tare.tools.research`** | Acervo imutável de ADRs, papers, benchmarks e especificações publicadas. | Nenhuma dependência de tempo de execução. | Markdown / Static Site (GitHub Pages). |

### 2.2 Grafo Dirigido Acíclico de Dependências (Auditável em CI)

```
        ┌────────────────────────────────────────────────────────┐
        │                      tare-kernel                       │
        └──────────────┬───────────────────┬─────────────────────┘
                       │                   │
         [CLI/IPC v1]  │                   │ [Query Engine]
                       ▼                   ▼
       ┌──────────────────────┐    ┌──────────────────────┐
       │  tare-backlog-graph  │    │    tare-specgraph    │
       │(Zero Dep. Externas)  │    │  (Consome Git Trees) │
       └──────────────────────┘    └──────────────────────┘
                       ▲                   ▲
                       │ (Apenas Documentação / Specs)
                       │                   │
        ┌────────────────────────────────────────────────────────┐
        │                  tare.tools.research                   │
        └────────────────────────────────────────────────────────┘
```

* **Tratamento de Metadados Opacos no Backlog:** O `tare-backlog-graph` armazena `spec_ids`, `git_commit_sha` e `envelope_sha256` exclusivamente como strings opacas de metadados. Não executa verificações de existência no Git nem em specs, preservando o isolamento hermético.
* **Validação Unidirecional de Specs:** A validação de conformidade e existência de specs cabe ao `tare-kernel` na fase **Prepare**, consultando unidirecionalmente o `tare-specgraph`.
* **Verificação Automatizada em CI:** Todo PR em qualquer repositório roda o analisador estático de fronteiras (`boundary-lint`), reprovando qualquer importação reversa ou transversal não autorizada.

---

## 3. Protocolo de Landing Idempotente, Saga FSM & Crash Recovery Robusto

O landing de um trem é modelado como uma **Saga Idempotente de 2 Fases com Write-Ahead Journal (WAL)** e verificação de evidência por **alcançabilidade de histórico (*reachability probe*)**.

```
                  ┌───────────────────────────────┐
                  │          1. PREPARED          │ ◄── Envelope RFC 8785 & Assinatura Notary
                  └──────────────┬────────────────┘
                                 │
                                 ▼ (fsync no WAL local)
                  ┌───────────────────────────────┐
                  │          2. COMMITTED         │ ◄── Serializador de Branch (Lock com Lease/TTL)
                  └──────────────┬────────────────┘
                                 │
                                 ▼ (git push atômico com verificação de base)
                  ┌───────────────────────────────┐
                  │           3. PUSHED           │ ◄── Pousou no Remoto (Ponto de Não-Retorno)
                  └──────────────┬────────────────┘
                                 │
                                 ▼ (graph_ops.py land com train_id deduplicável)
                  ┌───────────────────────────────┐
                  │         4. RECONCILED         │ ◄── Landing Record gravado & Lock liberado
                  └───────────────────────────────┘
```

### 3.1 Write-Ahead Journaling (WAL) e Contrato de Fases

Antes de qualquer efeito colateral externo, o daemon do `tare-kernel` persiste e sincroniza em disco (`fsync`) o arquivo de journal:  
`.tare/journal/<train_id>.json` contendo o payload completo do `ReleaseEnvelope`.

### 3.2 FSM de Transições e Recuperação de Falhas por Alcançabilidade

Para evitar falsos negativos quando a branch avança concorrentemente após o push de um trem, o kernel avalia o estado externo através de um **predicado unificado de alcançabilidade (*Reachability Probe*)**:

$$\text{Probe}(T) \iff \left(\exists\, c \in \text{Ancestors}(\text{origin}/\text{target\_branch}) \mid \text{Trailer}(c, \text{"Tare-Train-Id"}) = T.\text{train\_id}\right)$$

A verificação exige correspondência quádrupla estrita entre:
1. SHA do commit $c$.
2. Trailer `Tare-Train-Id` $\equiv T.\text{train\_id}$.
3. Trailer `Tare-Envelope-Sha256` $\equiv \text{SHA-256}(\text{JCS}(T.\text{envelope}))$.
4. Trailer `Tare-Resulting-Tree` $\equiv T.\text{resulting\_tree\_hash}$.

| Estado no WAL | Evidência Externa (Reachability Probe & Backlog) | Ação de Recuperação no Boot / Restart |
| :--- | :--- | :--- |
| **`PREPARED`** | Probe **Falso** (Commit inexistente no Git remoto). | Descarta o envelope pendente ou reapresenta no Cockpit. O lease lock expira por TTL ou é liberado. |
| **`COMMITTED`** | **Probe Falso:** Commit não alcançável no remoto.<br>**Probe Verdadeiro:** Commit alcançável (push ocorreu antes do crash). | **Se Falso:** Aborta com segurança; nenhum efeito colateral ocorreu no DAG.<br>**Se Verdadeiro:** Promove o WAL para `PUSHED` e prossegue para reconciliação no backlog. |
| **`PUSHED`** | Probe **Verdadeiro** (Ponto de Não-Retorno irreversível no Git). | Reenvia requisição transacional atômica a `graph_ops.py land` com `reconciliation_mode: RECONCILE_RECORDED_INTENT`. |
| **`RECONCILED`** | `landing_records` do backlog já contém o `train_id`. | **No-op idempotente:** Arquiva o journal local e remove o lock de branch. |

### 3.3 Regras de Serialização, Rebase e Prevenção de Lock Stealing Descoordenado
1. **Serializador por Branch com Lease:** O landing adquire lease de 60 segundos (`lease_id`, renovável via heartbeat periódico).
2. **Proibição de Lock Stealing Cego com Journal Ativo:** Se um nó encontrar um lock expirado mas constatar a existência de um journal em estado `COMMITTED` ou `PUSHED` para a mesma branch, é **estritamente proibido roubar o lock e iniciar novo pouso**. O nó concorrente deve assumir o papel de *cooperative recovery worker*, executando a verificação de alcançabilidade do trem anterior e conduzindo-o até `RECONCILED` ou cancelamento seguro antes de adquirir o lock.
3. **Comparação Atômica Pré-Push:**
   O push atômico é executado estritamente se o HEAD remoto coincidir com o `base_commit_hash` atestado no envelope:
   ```bash
   git push origin <commit_hash>:refs/heads/<target_branch>
   ```
4. **Invalidação Obrigatória por Rebase:** Se a branch de destino avançar antes do push, **o Envelope é imediatamente invalidado**. Rebase ou recomputação de patch exige regeneração do Envelope e nova assinatura no Cockpit. É proibido auto-rebase em voo no momento do push.

### 3.4 Git Trailers Canônicos

Todo commit gerado e aterrissado pelo kernel incorpora trailers padronizados e auditáveis na mensagem de commit:

```gitcommit
feat(auth): add ed25519 token signature verification

Tare-Train-Id: train-20260818-auth-f4b2
Tare-Envelope-Sha256: 7f83b1657ff1fc53b92dc18148a1d65dfc2d4b1fa3d677284addd200126d9069
Tare-Expected-Dag-Rev: a3f89e21
Tare-Resulting-Tree: 4b825dc642cb6eb9a060e54bf8d69288fbee4904
Tare-Spec-Ids: SPEC-042, SPEC-088
```

### 3.5 Contrato de Interface CLI/IPC (`graph_ops.py` v1) e Reconciliação Atômica

A comunicação entre `tare-kernel` e `tare-backlog-graph` utiliza payload tipado em JSON Schema / Pydantic v2.

```bash
python graph_ops.py land --payload '<JSON_STRING>'
```

**Schema JSON de Entrada (`LandingRequestV1`):**
```json
{
  "protocol_version": "1.0",
  "train_id": "train-20260818-auth-f4b2",
  "task_ids": ["TASK-101", "TASK-102"],
  "expected_dag_rev": "a3f89e21",
  "git_commit_sha": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
  "envelope_sha256": "7f83b1657ff1fc53b92dc18148a1d65dfc2d4b1fa3d677284addd200126d9069",
  "reconciliation_mode": "ATOMIC_CAS"
}
```

* `reconciliation_mode`: `"ATOMIC_CAS"` (fluxo padrão) ou `"RECONCILE_RECORDED_INTENT"` (fluxo de recuperação pós-push verificado).

**Persistência Interna de `landing_records` no Backlog:**  
O backlog mantém uma tabela de idempotência interna no `work-graph.json`:
```json
"landing_records": {
  "train-20260818-auth-f4b2": {
    "git_commit_sha": "e3b0c44298fc...",
    "envelope_sha256": "7f83b165...",
    "task_ids": ["TASK-101", "TASK-102"],
    "applied_at_dag_rev": "a3f89e21",
    "timestamp_utc": "2026-08-18T16:45:00Z"
  }
}
```

**Semântica de Resposta do Backlog:**
* **`Exit 0 (SUCCESS)`:** Transição aplicada com sucesso sob `expected_dag_rev`. Tarefas marcadas como `DONE`, novo `landing_record` persistido, `new_dag_rev` gerado.
* **`Exit 0 (ALREADY_APPLIED)`:** O `train_id` já consta em `landing_records`. Operação puramente idempotente; retorna `current_dag_rev` sem alterar o grafo.
* **`Exit 0 (RECONCILED_UNDER_NEW_BASE)`:** Em modo `RECONCILE_RECORDED_INTENT`, se `expected_dag_rev` divergiu mas as `task_ids` continuam elegíveis, pertencentes ao trem e com dependências satisfeitas: transiciona as tarefas para `DONE`, registra o `landing_record` e gera `new_dag_rev`.
* **`Exit 1 (CAS_MISMATCH)`:** Modo `ATOMIC_CAS` rejeitado porque `expected_dag_rev` divergiu. Nenhuma mutação ocorre.
* **`Exit 1 (RECONCILIATION_CONFLICT)`:** Modo `RECONCILE_RECORDED_INTENT` rejeitado porque uma ou mais `task_ids` sofreram mutação concorrente incompatível (ex.: tarefa cancelada, reatribuída a outro trem ou dependência violada). **O grafo não é corrompido; o kernel paralisa o trem e gera alerta no Cockpit para intervenção humana.**
* **`Exit 2 (VALIDATION_ERROR)`:** Payload inválido perante o schema Pydantic v2.

---

## 4. Release Envelope, Serialização Canônica RFC 8785 & Custódia Notary

### 4.1 Definição Formal do Envelope e Serialização Canônica JCS
$$\text{ReleaseEnvelope} = \left\langle
\begin{array}{l}
\text{protocol\_version},\ \text{key\_id},\ \text{train\_id},\ \text{spec\_ids},\ \text{task\_ids}, \\
\text{base\_tree\_hash},\ \text{patch\_sha256},\ \text{resulting\_tree\_hash},\ \text{test\_evidence\_hash}, \\
\text{expected\_dag\_rev},\ \text{timestamp\_utc},\ \text{notary\_signature}
\end{array}
\right\rangle$$

Para garantir determinismo criptográfico byte-a-byte:
1. **Canonicidade JSON (RFC 8785):** O payload do envelope (excluindo o campo de assinatura) é serializado segundo o padrão **JSON Canonicalization Scheme (JCS / RFC 8785)**.
2. **Escopo da Assinatura:** $\text{Sig} = \text{Ed25519\_Sign}(\text{PrivKey},\ \text{SHA-256}(\text{JCS}(\text{EnvelopePayload})))$.
3. **Amarração de Chave:** O envelope declara explicitamente o `key_id` público utilizado.

### 4.2 Modelo de Custódia de Chaves Notary (Ed25519)
1. **Armazenamento Seguro:** Chaves privadas Ed25519 são mantidas no keyring nativo do SO ou em `.tare/identity/notary.key` (`0600`).
2. **Assinatura no Cockpit:** Ao acionar **"Approve & Land"** no Web Cockpit (:8765), o payload JCS é gerado e assinado pelo Notary.
3. **Verificação Descentralizada:** O `tare-kernel` valida a assinatura contra `.tare/authorized_notaries.json` antes de permitir transição para `COMMITTED`.
4. **Rotação:** Suporte a múltiplas chaves públicas ativas com revogação explícita por hash e identificador de chave (`key_id`).

---

## 5. Happy Path de Desenvolvimento em 3 Comandos

```
[ 1. `tare task new "Título" --spec SPEC-XX` ] ──► [ 2. `tare run` ] ──► [ 3. `tare approve <ID>` ]
• Cria a tarefa no DAG local (Backlog).             • O swarm despacha o Relay      • Operador revisa diff, atestação
• Registra metadados da spec (opacos).               Mesh (Plan → Code → Test).      e aciona a FSM de landing com 1 clique.
```

---

## 6. Estratégia de Coexistência, Replay Gravado & Bateria de 6 Falsificadores

```
┌───────────────────────────────────────────────────────────────────────────────────────────────────┐
│                   PIPELINE DE REPLAY HISTÓRICO DETERMINÍSTICO (CUTOVER GATE)                      │
└───────────────────────────────────────────────────────────────────────────────────────────────────┘
                                                  │
             ┌────────────────────────────────────┴───────────────────────────────────┐
             │                                                                        │
             ▼                                                                        ▼
┌──────────────────────────┐                                            ┌──────────────────────────┐
│  Artefato Gravado v1     │                                            │     tare-kernel v2       │
│ • patch_sha256 histórico │ ───► [ Aplicação de Patch no Sandbox ] ──► │ • Verificação Tree Hash  │
│ • Inputs de Teste Grav.  │ ───► [ Execução do Harness de Teste  ] ──► │ • Atestação Notary JCS   │
│ • DAG Rev Original       │ ───► [ graph_ops.py land v1          ] ──► │ • Transição DAG Idemp.   │
└──────────────────────────┘                                            └──────────────────────────┘
                                                                                      │
                                                                                      ▼
                                                                        ┌──────────────────────────┐
                                                                        │  Critério de Aceite:     │
                                                                        │  • Resulting Tree ID     │
                                                                        │  • DAG Concluído         │
                                                                        │  • Zero Re-inferência    │
                                                                        └──────────────────────────┘
```

### 6.1 Replay de Paridade com Artefatos Gravados (Sem Re-inferência de LLM)
O gate de cutover v1 $\rightarrow$ v2 valida a **plumbing determinística** do kernel:
1. **Escopo do Replay:** Execução de 10 trens históricos representativos gravados no v1.
2. **Critérios Estritos de Equivalência:**
   * $\text{resulting\_tree\_hash}_{\text{v2}} \equiv \text{resulting\_tree\_hash}_{\text{v1}}$.
   * Identidade binária em `test_evidence_hash`.
   * Reconciliação correta no grafo com idempotência estrita em reexecuções.
   * **Exclusão Formal:** Re-inferência de LLMs é categoricamente proibida no teste de paridade.

### 6.2 Bateria dos 6 Falsificadores Executáveis Obrigatórios (Fase 1)

Antes da migração de tráfego de produção, o sistema deve ser aprovado nos 6 testes adversariais:

1. **`TEST-FALSIFIER-CRASH-PUSH` (Injeção de Falha Pós-Push / Pré-Backlog):**
   * *Cenário:* `kill -9` no daemon do kernel imediatamente após o sucesso de `git push` e antes da chamada a `graph_ops.py`.
   * *Critério de Sucesso:* No boot, o recovery detecta o commit no remoto via Reachability Probe, reenvia `graph_ops.py land` com `reconciliation_mode: RECONCILE_RECORDED_INTENT` e conclui a aterrissagem sem duplicar transições nem travar o DAG.
2. **`TEST-FALSIFIER-CRASH-PRE-WAL-UPDATE` (Crash na Janela Push $\rightarrow$ WAL com Avanço Concorrente de Branch):**
   * *Cenário:* `kill -9` imediatamente após `git push`, antes da gravação de `PUSHED` no WAL. O lease expira e um segundo trem (`T2`) pousa na branch remota, tornando o commit de `T1` um ancestral não-HEAD.
   * *Critério de Sucesso:* No restart, o recovery de `T1` executa o Reachability Probe, localiza o commit de `T1` nos ancestrais do HEAD remoto, promove o WAL para `PUSHED` e reconcilia o backlog com sucesso.
3. **`TEST-FALSIFIER-CAS-COLLISION` (Avanço Concorrente de DAG sob Modo CAS):**
   * *Cenário:* Dois trens (`T1` e `T2`) iniciam sob o mesmo `dag_rev`. `T1` aterrissa primeiro. `T2` tenta pousar em modo `ATOMIC_CAS`.
   * *Critério de Sucesso:* `T2` recebe rejeição imediata com código `CAS_MISMATCH`; o backlog permanece intacto; `T2` é forçado a revalidar o estado.
4. **`TEST-FALSIFIER-CONCURRENT-BACKLOG-MUTATION` (Crash Pós-Push com Mutação Concorrente no Backlog):**
   * *Cenário:* `T1` efetua push com sucesso e cai. Antes do restart, uma intervenção cancela `TASK-101` (pertencente a `T1`) no backlog. No boot, `T1` tenta reconciliação pós-push.
   * *Critério de Sucesso:* O backlog detecta a incompatibilidade semântica de estado, rejeita a operação com `409 RECONCILIATION_CONFLICT`, não corrompe o DAG e encaminha o evento para resolução humana no Cockpit.
5. **`TEST-FALSIFIER-SPEC-ISOLATION` (Zero-Dependência do Backlog):**
   * *Cenário:* Criação de tarefa com `--spec SPEC-INEXISTENTE-999`.
   * *Critério de Sucesso:* O backlog registra a tarefa tratando a spec como metadado opaco. O `tare-kernel`, durante a fase **Prepare**, consulta o `tare-specgraph` e bloqueia a formação do `ReleaseEnvelope` até a correção da referência.
6. **`TEST-FALSIFIER-BACKLOG-NO-GIT-SANDBOX` (Execução Hermética do Backlog sem Git):**
   * *Cenário:* Execução de `graph_ops.py land` em container sandbox totalmente desprovido do binário `git` e de variáveis de ambiente do repositório.
   * *Critério de Sucesso:* `graph_ops.py` executa transições atômicas, validações CAS e gravações de `landing_records` com 100% de taxa de sucesso e zero chamadas a subsistemas externos.

---

## 7. Roadmap de Lançamento em 4 Fases

```
┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                   ROADMAP DE IMPLEMENTAÇÃO 2.0                                     │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

 ┌──────────────────────────┐     ┌──────────────────────────┐     ┌──────────────────────────┐
 │  Fase 1: Engine P0       │     │  Fase 2: Cockpit & CLI   │     │  Fase 3: SpecGraph Intel │
 │  (Semanas 1-2)           │ ──► │  (Semanas 3-4)           │ ──► │  (Semanas 5-6)           │
 │ • 4 Repos segregados     │     │ • CLI `tare` unificada   │     │ • CPG AST Parser         │
 │ • WAL FSM & Reachability │     │ • Web Cockpit (:8765)    │     │ • Drift Gate em PRs      │
 │ • Notary Ed25519 (JCS)   │     │ • Monitoramento Swarm    │     │ • Context Envelopes      │
 │ • Chaos WAL Injector     │     │ • Aprovação em 1 clique  │     │ • Matriz Causal Fechada  │
 │ • 6 Falsificadores & Repl│     │                          │     │                          │
 └──────────────────────────┘     └──────────────────────────┘     └──────────────────────────┘
                                                                                 │
                                                                                 ▼
                                                                   ┌──────────────────────────┐
                                                                   │  Fase 4: Multi-Node Mesh │
                                                                   │  (Semanas 7-8)           │
                                                                   │ • Workers remotos        │
                                                                   │ • Tailscale Mesh         │
                                                                   │ • Despacho Distribuído   │
                                                                   └──────────────────────────┘
```

1. **Fase 1 (Greenfield Engine, FSM WAL, Notary RFC 8785 & Chaos Boot P0):**
   * Inicialização dos 4 repositórios segregados (`tare-backlog-graph`, `tare-kernel`, `tare-specgraph`, `tare.tools.research`).
   * Implementação da Saga FSM com WAL em disco (`fsync`), lease lock por branch e protocolo de recuperação por *Reachability Probe*.
   * Validação tipada estrita em `graph_ops.py` (Pydantic v2) e persistência de `landing_records`.
   * Geração e verificação de assinaturas Ed25519 sobre payloads canônicos JCS (RFC 8785).
   * Execução e aprovação nos testes com *Chaos Injector* (interrupção SIGKILL em todos os offsets de WAL), Replay Gravado dos 10 trens históricos e nos **6 Falsificadores Executáveis Obrigatórios**.
2. **Fase 2 (CLI Unificada & Web Cockpit 1-Click):**
   * Interface de linha de comando ergonômica (`tare task new`, `tare run`, `tare approve`).
   * Web Cockpit local (:8765) para visualização em tempo real de trens, diffs, atestação e botão de aterrissagem atômica.
3. **Fase 3 (SpecGraph Integration & Causal Drift Gate):**
   * Motor de CPG AST para rastreabilidade bidirecional (Specs $\leftrightarrow$ Code $\leftrightarrow$ Tests).
   * Context Envelopes dinâmicos e Drift Gate bloqueante em PRs quando a matriz causal divergir.
4. **Fase 4 (Multi-Node Compute & Distributed Swarm Mesh):**
   * Conexão de nós de execução remota heterogêneos via túneis seguros (Tailscale).
   * Despacho distribuído de tarefas de compilação pesada e suítes de teste distribuídas.
```
